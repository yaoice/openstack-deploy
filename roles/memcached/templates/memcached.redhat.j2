# {{ ansible_managed }}

{% if debug | bool %}
  {% set _verbosity = '-vvv' %}
{% else %}
  {% set _verbosity = '-v' %}
{% endif %}

PORT="{{ memcached_port }}"
USER="{{ memcached_user }}"
MAXCONN="{{ memcached_connections }}"
CACHESIZE="{{ memcached_memory }}"
OPTIONS="-l {{ api_interface_address }} -t {{ memcached_threads }} {{ _verbosity }}"
